# 商印项目环境变量配置
# 复制此文件为 .env 并根据实际情况修改

# 环境配置
NODE_ENV=production

# 数据库配置 (MySQL)
DB_DIALECT=mysql
DB_HOST=82.156.83.99
DB_PORT=3306
DB_USER=shangyin
DB_PASS=shaoyansa
DB_NAME=shangyin

# JWT 密钥 (生产环境请修改为随机字符串)
JWT_SECRET=shangyin_jwt_secret_$(openssl rand -hex 32)

# 端口配置
PORT=3000
